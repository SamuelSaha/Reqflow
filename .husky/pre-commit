#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Running pre-commit security checks..."

# Run lint-staged for code quality
npx lint-staged

# Optional: Run pre-commit framework if installed
# Uncomment the following line if you want to use the Python pre-commit framework
# if command -v pre-commit &> /dev/null; then pre-commit run; fi

echo "✅ Pre-commit checks passed"
