import { PageShell, ComingSoon } from "@/components/layout";

export default function ChangelogPage() {
  return (
    <PageShell>
      <ComingSoon
        title="Changelog"
        description="Track every update, improvement, and fix as we build Reqflow."
      />
    </PageShell>
  );
}
